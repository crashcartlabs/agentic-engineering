interface:
  display_name: "Handoff"
  short_description: "Compact the current conversation into a handoff document so a fresh session can resume the work without re-deriving context."
  default_prompt: "Use $handoff to help with this task."

policy:
  allow_implicit_invocation: false
