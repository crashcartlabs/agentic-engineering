interface:
  display_name: "Security Audit"
  short_description: "Use the Security Audit workflow in this task."
  default_prompt: "Use $security-audit to help with this task."

policy:
  allow_implicit_invocation: false
