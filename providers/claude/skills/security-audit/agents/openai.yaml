interface:
  display_name: "Security Audit"
  short_description: "Exploitability review of the current worktree's changes — hunts for vulnerabilities the diff introduced or newly exposed, not correctness…"
  default_prompt: "Use $security-audit to help with this task."

policy:
  allow_implicit_invocation: false
