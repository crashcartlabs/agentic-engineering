# TODO

- File an issue: delete the "Migration from the pre-tracked-report flow" paragraphs
  from `review-plan`, `security-audit`, and `skill-safety-scan` once the transition
  period ends (they exist only to clean up stale `info/exclude` lines from the
  pre-tracked-report era).
